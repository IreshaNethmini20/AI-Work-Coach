import React from 'react';
import CopyButton from '../components/ui/CopyButton';
import Icon from '../components/common/Icon';
export default function TaskAnalysis({ analysis }) { const level = analysis.aiOpportunity?.level?.toLowerCase() || 'medium'; const skill = analysis.skillToPractice || {}; return <article className="coaching-plan"><header className="plan-header"><span className="eyebrow"><Icon name="sparkles" size={15} /> YOUR COACHING PLAN</span><h1>AI Coaching Plan</h1><div className="original-task"><span>Your task</span><p>{analysis.task}</p></div></header><section className="opportunity"><span className={`opportunity-level ${level}`}>{analysis.aiOpportunity.level} AI OPPORTUNITY</span><p>{analysis.aiOpportunity.message}</p></section><div className="plan-grid"><section className="workflow-section"><div className="section-heading"><span className="section-icon"><Icon name="target" /></span><div><h2>Recommended workflow</h2><p>A responsible way to use AI for this work.</p></div></div><ol className="workflow">{analysis.recommendedWorkflow.map((step, index) => <li key={step.step}><span className="workflow-number">{String(index + 1).padStart(2, '0')}</span><div><h3>{step.title}</h3><p>{step.description}</p></div></li>)}</ol></section><aside className="plan-aside"><section className="skill-card"><span className="section-caption">SKILL YOU'RE PRACTISING</span><h2>{skill.name}</h2><p className="skill-level">{skill.level === 'Intermediate' ? 'Beginner to Building' : skill.level}</p><p>This task builds the habit of giving AI clear context, checking its output, and applying your own work judgement.</p></section><section className="impact-card"><span className="section-caption">POTENTIAL BENEFIT</span><p>{analysis.expectedBenefit}</p></section></aside></div><section className="prompt-panel"><div className="prompt-heading"><div><span className="section-caption">READY TO USE</span><h2>Your ready-to-use prompt</h2><p>Take this to ChatGPT, Claude, Copilot, or your approved AI assistant.</p></div><CopyButton text={analysis.readyToUsePrompt} /></div><blockquote>{analysis.readyToUsePrompt}</blockquote></section><section className="review-callout"><span><Icon name="shield" size={21} /></span><div><h2>Keep human judgement here</h2><p>{analysis.humanReview}</p></div></section></article>; }
