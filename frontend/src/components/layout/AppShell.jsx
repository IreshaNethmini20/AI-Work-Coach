import React, { useState } from 'react';
import Icon from '../common/Icon';
export default function AppShell({ children }) { const [open, setOpen] = useState(false); return <div className="app-shell"><button className="mobile-menu" onClick={() => setOpen(!open)} aria-label={open ? 'Close navigation' : 'Open navigation'}><Icon name={open ? 'close' : 'menu'} /></button><aside className={`sidebar ${open ? 'sidebar-open' : ''}`}><a href="/" className="brand" onClick={() => setOpen(false)}><span className="brand-mark"><Icon name="sparkles" size={20} /></span><span>AI Work Coach</span></a><p className="workspace-label">WORKSPACE</p><div className="nav-item active"><Icon name="coach" size={18} /><span>Coach</span></div><div className="profile"><div className="avatar">IN</div><div><strong>Iresha Nethmini</strong><span>Employee</span></div></div></aside><main className="workspace">{children}</main></div>; }
