# Components

Reusable React components.

## Subdirectories

- `common/` - Small reusable components used across multiple parts of the application
- `layout/` - Application-wide layout components (Header, Footer, Sidebar, PageContainer, etc.)
- `ui/` - Reusable interface components (Button, Card, Input, Badge, Modal, LoadingState, etc.)
